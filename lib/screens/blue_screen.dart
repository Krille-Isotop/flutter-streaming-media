import 'package:flutter/material.dart';

class BlueScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('BlueScreen')
      ),
      body: Center(
        child: Text('BlueScreen')
      ),
    );
  }
}